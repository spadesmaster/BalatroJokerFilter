# Changelog

## 0.5.4
- Reduced the Slot pill width by roughly 15% so the label stays readable without taking too much space.
- Removed Show All from config because All should always remain visible.
- Reorganized the in-game Show-button toggles into 3 rows of 3.
- Synced version numbers across Lua, JSON, config, and changelog.

## 0.5.3
- Compacted the in-game Config tab so it takes less vertical space.
- Grouped show-button toggles into 3-per-row layout where possible.
- Widened the Slot pill so its text stays readable.

## 0.5.2
- Cleaned up the main.lua header so the description, pill definitions, and notes match the current feature set.
- Updated Slot to show `count (x free)` or `count (x over)`.
- Hide zero-count special pills while keeping All, Slot, and Rarity visible.
- Synced version numbers across main.lua, joker_filter.json, and CHANGELOG.md.
